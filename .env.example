# Application
APP_NAME=AES
NEXT_PUBLIC_APP_NAME=AES

# Database
MONGODB_URI=mongodb://localhost:27017/aes

# Authentication
JWT_SECRET=change-me-to-a-32-plus-char-random-string
JWT_EXPIRES_IN=7d

# Encryption (32-byte hex — generate with: openssl rand -hex 32)
AES_ENCRYPTION_KEY=change-me-to-a-64-char-hex-string-32-bytes

# ZeroClaw Agent Runtime
ZEROCLAW_BINARY_PATH=/usr/local/bin/zeroclaw

# File System
ARTIFACTS_ROOT=/artifacts

# LLM
OLLAMA_ENDPOINT=http://localhost:11434

# Admin Seed
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123!

# Ports
BACKEND_PORT=3001
FRONTEND_PORT=3000

# URLs
BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_BACKEND_WS_URL=ws://localhost:3001
